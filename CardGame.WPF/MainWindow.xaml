<Window x:Class="CardGame.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardGame.WPF"
        xmlns:engine="clr-namespace:CardGame.Engine;assembly=CardGame.Engine"
        d:DataContext="{d:DesignInstance {x:Type engine:MainWindowViewModel}}"
        mc:Ignorable="d"
        Title="Card Game" Height="150" Width="350">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Enter cards:" FontWeight="Bold"/>
        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Width="200"
                 Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" />

        <Button Grid.Row="1"
                Grid.ColumnSpan="2"
                Content="Calculate"
                Command="{Binding ComputeScoreCommand}"
                Margin="0,10,0,0"/>

        <TextBlock Grid.Row="3"
                   Foreground="Green"
                   Visibility="{Binding HasScore, Converter={StaticResource BoolToVis}}"
                   Text="{Binding Score, StringFormat='Score: {0}'}" />

        <TextBlock Grid.Row="4"
                   Grid.ColumnSpan="2"
                   Foreground="Red"
                   Text="{Binding Error}" />
    </Grid>
</Window>
